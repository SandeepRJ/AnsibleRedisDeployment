# DevopsTask

Change the hosts to the ip of the host that is available
The connection variables are also to be changed accordingly or use localhost for local deployment

The monitoring module in ansible is not setup properly as of now.
 
